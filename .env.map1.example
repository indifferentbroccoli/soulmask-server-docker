# Required
PUID=1000
PGID=1000

# IMPORTANT: SERVER_PASSWORD must be identical on both map servers.
SERVER_NAME=My Soulmask Server - Cloud Mist Forest
SERVER_PASSWORD=changeme
ADMIN_PASSWORD=
GAME_MODE=pve
GAME_WORLD=Level01_Main
MAX_PLAYERS=20

GAME_PORT=8777
QUERY_PORT=27015
ECHO_PORT=18888
LISTEN_ADDRESS=0.0.0.0

SAVING_INTERVAL=600
BACKUP_INTERVAL=900

UPDATE_ON_START=true

# =============================================================================
# Cross-Map — Main server configuration
# This server acts as the main server: it hosts the character database and
# broadcasts on CROSS_SERVER_MAIN_PORT for child servers to connect.

SERVER_ID=1
CROSS_SERVER_MAIN_PORT=20000
